def HELLO="hola mundo"

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script{
                    checkout scm
                    def constants = load 'jenkins/constants.groovy'
                    echo "${constants.gitId}"
                }
                
            }
        }
    }
}
