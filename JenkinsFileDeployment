def HELLO="hola mundo"

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                script{
                    checkout scm
                    def constants = load 'constants.groovy'
                    echo "${constants.gitId}"
                }
                
            }
        }
    }
}
